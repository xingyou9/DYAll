# DYAll 抖音聚合版 3.0.0 — 合并说明

## 这是什么
基于开源插件 DYYY（github.com/huami1314/DYYY，含全部功能源码）改造的**单一 dylib 聚合版**，
品牌/包名改为 DYAll，并新增了液态玻璃主题界面（DYAllGlassUI.xm）。

## 做了什么改动
1. `Makefile`：TWEAK_NAME = DYAll，编译产物为 **DYAll.dylib**
2. `control`：包名 com.aggregate.dyall，名称"抖音聚合版 DYAll"，版本 3.0.0
3. 新增 `DYAllGlassUI.xm`：
   - 给 DYYY 设置界面加 iOS 26+ 原生液态玻璃背景（运行时探测 UIGlassEffect）
   - iOS 26 以下自动回退系统毛玻璃（UIBlurEffect systemChromeMaterial）
   - 默认开启；注入后在偏好里写 DYAllGlassUIDisabled=YES 可关闭
4. `.github/workflows/build.yml`：保留原作者云编译工作流（一次编译 rootful/rootless/roothide 三种 deb），
   产物名改为 DYAll，并新增 DYAll.dylib 单文件产物上传（全能签注入用）
5. 所有文件重命名检查通过：viewDidLoad 钩子方法存在、%hook/%end 配对正确

## 怎么编译出 DYAll.dylib（本机无编译环境，用 GitHub 免费云编译）
1. 注册/登录 GitHub，新建一个仓库（如 DYAll）
2. 把本文件夹全部内容上传到该仓库（包含 .github 文件夹！）
3. 打开仓库 Actions 页 → 选中 "build deb" → Run workflow
4. 等待编译完成（约10-20分钟）→ 在该次运行的 Artifacts 里下载：
   - `DYAll`：三种越狱安装包（deb）
   - `DYAll-dylib`：**DYAll.dylib 单文件**（全能签直接注入这个）
5. 国内访问 GitHub 可用 ghproxy.net 等镜像加速

## 功能覆盖与诚实边界
- ✅ DYAll.dylib = DYYY 全部功能（776+ 设置项：界面净化/过滤/下载/复制/透明度/液态玻璃主题）
- ⚠️ DouyinHelper（抢红包/防撤回）为闭源插件，无源码可合并，无法并入 DYAll。
  需要它的话，把 DYAll.dylib 与 DouyinHelper.dylib 一起注入即可（注意重叠功能只开一边）
- ⚠️ 其余闭源插件（AutoMessagePlugin、Vcam、AWEVideoInfoCopy、DYKiller 等）同理，
  只能以"注入多个文件"的方式与 DYAll 共存，无法在源码层合并
- ⚠️ 编译是否成功取决于 Theos/SDK 环境与 DYYY 源码本身，以 Actions 运行结果为准
